# Server-side Next.js calls use this to reach the FastAPI app directly.
# Local development defaults to http://127.0.0.1:8000 if this is omitted.
# In production, point this at your deployed API origin.
API_BASE_URL=https://api.your-domain.com
